import { Meta, StoryObj } from '@storybook/react'
import { Container } from '.'

export default {
  title: 'Container',
  component: Container,
  args: {
    children: 'Container'
  }
} as Meta

export const Default: StoryObj = {}
